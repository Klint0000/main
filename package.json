{
  "name": "calculator",
  "version": "1.0.0",
  "description": "created by klint",
  "source": "cal.html",
  "scripts": {
    "dev": "parcel",
    "build": "parcel build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "klint",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.12.0"
  }
}
